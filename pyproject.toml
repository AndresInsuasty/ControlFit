[project]
name = "controlfit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.32.0",
    "gspread>=6.0.0",
    "pandas>=2.0.0",
    "plotly>=5.18.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
